/*
Event: something that has happened in our app that we can respond to.  In node we actually talk about two different kinds of events.
*/

var EventEmitter = require('events');

console.log(EventEmitter);